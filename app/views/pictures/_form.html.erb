<%= form_with(model: picture) do |form| %>
  <% if picture.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(picture.errors.count, "error") %> prohibited this picture from being saved:</h2>

      <ul>
        <% picture.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :imageable_id, style: "display: block" %>
    <%= form.number_field :imageable_id %>
  </div>

  <div>
    <%= form.label :imageable_type, style: "display: block" %>
    <%= form.text_field :imageable_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
